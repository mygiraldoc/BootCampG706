# Guía de implementación del chatbot(supervisado)

ejemplo colab
import nltk
nltk.download('punkt')
python -c "importnltk.dowload('punkt')"
python -m pip install nltk

python
import nltk
nltk.download('punkt')

Instalar librerias
`pip install scikit-learn nltk numpy gym==0.26.2 gym-notices`

